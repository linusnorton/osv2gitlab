{
  "name": "osv2gitlab",
  "version": "1.0.0",
  "description": "Convert osv-scanner report JSON to GitLab dependency scanner report format",
  "main": "index.js",
  "bin": "index.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node index.js",
    "test": "npm install -g ajv-cli && node index.js report.json > output.json && npx ajv --strict=false -s schema.json -d output.json"
  },
  "private": false
}
